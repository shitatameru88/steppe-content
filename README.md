# Steppe Mysteries — remote cases

Cases #4 onwards live here and are downloaded by the app (no app update needed).
Served by Cloudflare Pages (primary) and GitHub Pages (fallback). Generated by `Tools/export_case.py` —
don't edit by hand. Publishing = commit + push this folder's repo.
